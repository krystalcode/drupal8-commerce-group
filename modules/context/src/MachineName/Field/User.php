<?php

namespace Drupal\gcommerce_context\MachineName\Field;

/**
 * Holds machine names of User entity fields.
 */
class User {

  /**
   * Holds the default context for the user.
   */
  const DEFAULT_CONTEXT = 'gcommerce_context_default';

  /**
   * Holds whether to remember the last used context next time the user logs in.
   */
  const REMEMBER_CONTEXT = 'gcommerce_context_remember';

}
